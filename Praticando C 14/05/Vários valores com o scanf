#include <stdio.h>

int main(void) {
int num1, num2, num3;
  printf(" digite três números inteiros: ");
  scanf("%d%d%d", &num1, &num2, &num3);
  printf(" valores lidos: %d, %d e %d\n", num1, num2, num3);
  }
